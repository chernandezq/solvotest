import React from 'react';
import {View, Text} from 'react-native';

const Search: () => React = props => {
  return (
    <View style={{flex: 1}}>
      <Text style={{textAlign: 'center'}}>Hi Search</Text>
    </View>
  );
};

export default Search;
