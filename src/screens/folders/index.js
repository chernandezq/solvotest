import React from 'react';
import {View, Text} from 'react-native';

const Folders: () => React = props => {
  return (
    <View style={{flex: 1}}>
      <Text style={{textAlign: 'center'}}>Hi Folders</Text>
    </View>
  );
};

export default Folders;
